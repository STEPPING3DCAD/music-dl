services:
  music-dl:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8765:8765"
    volumes:
      - ${MUSIC_DL_CONFIG:-~/.config/music-dl}:/root/.config/music-dl
      - ${MUSIC_DIR:-./music}:/root/download
    restart: unless-stopped